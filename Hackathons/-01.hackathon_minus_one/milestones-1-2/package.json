{
  "name": "resume-builder",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && copyfiles -u 1 src/css/**/* public/ && copyfiles -u 1 src/assets/**/* public/assets && copyfiles -u 1 dist/js/**/* public/",
    "postbuild": "echo Copying static files... && copyfiles -u 1 src/static/**/* public/static",
    "start": "npx http-server public",
    "deploy": "vercel --prod"
  },
  "author": "Farah Syed",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.5.4"
  }
}
